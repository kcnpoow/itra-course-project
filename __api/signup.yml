info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER_URL}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "test2@gmail.com",
        "password": "password",
        "confirmPassword": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
