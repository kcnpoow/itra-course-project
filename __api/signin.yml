info:
  name: signin
  type: http
  seq: 3

http:
  method: POST
  url: "{{SERVER_URL}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "kcnpoow@gmail.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
