generator client {
  provider        = "prisma-client"
  previewFeatures = ["typedSql"]
  output          = "./generated"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
